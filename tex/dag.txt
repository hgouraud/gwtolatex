%doctype;
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
<!-- $Id: /etc/Tex/dag.txt,v TeX.1 2014-09-05 13:15:19 hg Exp $ -->
<!-- Copyright (c) 1998-2007 INRIA -->

%let;nc;%dag.ncol;%in;
%let;lll;%for;i;0;nc;l%end;%in;

\begin{tabular}{%lll;}
%foreach;dag_line;
  %foreach;dag_cell;
    %if;(not is_first) & %end;
    %if;(dag_cell.colspan > 1)\multicolumn{%dag_cell.colspan;}{
      %if;dag_cell.is_hr_left;l
      %elseif;dag_cell.is_hr_right;r
      %else;c
      %end;}{%end;
    %if;dag_cell.is_nothing; 
    %elseif;dag_cell.is_bar;|
    %elseif;(dag_cell.item != "")%dag_cell.item;
    %elseif;(dag_cell.text != "")%dag_cell.text;
    %else;
      %if;dag_cell.is_hr_left;\rule{\linewidth/2}{0.4pt}
      %elseif;dag_cell.is_hr_right;\rule{\linewidth}{0.4pt}
      %else;\rule{\linewidth}{0.4pt}
      %end;
    %end;
    %if;(dag_cell.colspan > 1)}%end;
  %end; \\
%end;
\end{tabular}
</body>
</html>
